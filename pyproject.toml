[project]
name = "citor-tooling"
version = "0.1.0"
description = "Dev tooling for citor (pre-commit, formatters)"
requires-python = ">=3.10"
dependencies = [
    "pre-commit>=3.5",
    "commitizen>=3.13",
    "gersemi>=0.16",
]

[tool.commitizen]
name = "cz_conventional_commits"
